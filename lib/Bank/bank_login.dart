import 'package:flutter/material.dart';

class BankLogin extends StatelessWidget {
  const BankLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
